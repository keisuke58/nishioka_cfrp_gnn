# 入出力仕様書 (M4-3)

## 推論 API

### 入力

| 項目 | 形式 | 説明 |
|------|------|------|
| ノード特徴 | `[N, 4]` float32 | (x, y, z, DSPSS_zscore) |
| エッジ | `[2, E]` int64 | メッシュ隣接（COO形式） |
| ノード数 | 13942 | 固定（2層メッシュ） |

### 出力

| 項目 | 形式 | 説明 |
|------|------|------|
| 位置予測 | `[N]` int64 | 19クラス（0=欠陥なし, 1-18=欠陥位置） |
| 検出 | 2値 | 0=欠陥なし, 1=欠陥あり（位置≠0のノードが存在） |
| サイズ（multi-task） | `[G]` int64 | 0=small, 1=medium, 2=large |
| 回帰（M3-1） | `[G, 6]` float32 | [φ, E_ratio, cx, cy, cz, size]（オプション） |

### エラーコード

| コード | 意味 |
|--------|------|
| 0 | 成功 |
| 1 | 入力形状不正 |
| 2 | モデル読み込み失敗 |
| 3 | デバイス（GPU）未検出 |

## データファイル形式

- **DSPSS**: `.npy` float32, shape `(13942,)`
- **ラベル**: `.npy` one-hot `(13942, 19)` または class id `(13942,)`

## モデルチェックポイント

- **形式**: PyTorch `.pth`
- **キー**: `model_state_dict` または 直接 `state_dict`
- **アーキテクチャ**: GATModel (hidden=16, two-stage) または GATMultiTaskModel
