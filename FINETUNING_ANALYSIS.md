# ファインチューニング結果分析と改善プラン

## 📊 現在の結果

### 性能比較
- **事前学習モデル**: Macro F1 = **0.730** (F10p730)
- **ファインチューニング後**: Macro F1 = **0.7249** (Epoch 12で最高)
- **テストセット**: Macro F1 = **0.6578**

### 問題点
1. ❌ **性能が下がっている**: 0.730 → 0.7249 (-0.0051)
2. ❌ **Early stoppingが早すぎる**: Epoch 12で最高、その後100エポック改善なし
3. ❌ **学習率が低すぎる可能性**: 0.0001 (元の0.001の1/10)
4. ❌ **OneCycleLRのmax_lrが低い**: 0.0002 (元の設定では0.002の2倍 = 0.004)

## 🔍 原因分析

### 1. 学習率の問題
- 元の学習: LR = 0.001, OneCycleLR max_lr ≈ 0.002
- ファインチューニング: LR = 0.0001, OneCycleLR max_lr = 0.0002
- **10倍低い学習率**で、モデルが十分に更新されていない可能性

### 2. Early Stoppingの問題
- Patience = 100エポック
- Epoch 12で最高性能後、100エポック改善なしで停止
- しかし、ファインチューニングでは初期の性能が既に良いため、改善の余地が少ない

### 3. ファインチューニング戦略の問題
- 全層を同じ学習率で更新
- バックボーンと分類ヘッドの学習率を分けていない
- バックボーンを凍結していない

## 🎯 改善プラン

### 戦略1: 層別学習率 + より高い学習率

```bash
# バックボーン: 低LR (0.0001), 分類ヘッド: 高LR (0.001)
BACKBONE_LR=0.0001 HEAD_LR=0.001 LR=0.001 EPOCHS=1000 PATIENCE=200 bash run_finetune.sh
```

**期待される効果**:
- 分類ヘッドをより積極的に更新
- バックボーンは既存の知識を保持しつつ微調整

### 戦略2: バックボーン凍結 + 分類ヘッドのみ学習

```bash
# バックボーンを凍結、分類ヘッドのみ学習
FREEZE_BACKBONE=1 HEAD_LR=0.001 LR=0.001 EPOCHS=500 PATIENCE=150 bash run_finetune.sh
```

**期待される効果**:
- バックボーンの知識を完全に保持
- 分類ヘッドのみ最適化してタスクに特化

### 戦略3: 段階的ファインチューニング

```bash
# Step 1: 分類ヘッドのみ学習（高LR）
FREEZE_BACKBONE=1 HEAD_LR=0.001 LR=0.001 EPOCHS=200 PATIENCE=50 bash run_finetune.sh

# Step 2: 全層ファインチューニング（低LR）
FREEZE_BACKBONE=0 BACKBONE_LR=0.00005 HEAD_LR=0.0005 LR=0.0005 EPOCHS=500 PATIENCE=150 bash run_finetune.sh
```

**期待される効果**:
- まず分類ヘッドを最適化
- その後、全層を微調整

### 戦略4: より高い学習率で全層更新

```bash
# 元の学習率に近い値でファインチューニング
LR=0.0005 EPOCHS=1000 PATIENCE=200 bash run_finetune.sh
```

**期待される効果**:
- より積極的な更新で性能向上の余地を確保

### 戦略5: CosineAnnealingLRを使用（OneCycleLRの代わり）

OneCycleLRは初期の学習率が低すぎる可能性があるため、CosineAnnealingLRを試す。

```bash
# スクリプトを修正して --no_onecycle を追加
LR=0.0005 EPOCHS=1000 PATIENCE=200 bash run_finetune.sh --no_onecycle
```

## 📋 推奨実行順序

1. **戦略2（バックボーン凍結）**: 最も安全で効果的
2. **戦略1（層別学習率）**: バックボーンも微調整したい場合
3. **戦略4（高学習率）**: より積極的な更新が必要な場合
4. **戦略3（段階的）**: 時間がある場合の最適化

## 🔧 実装が必要な改善

### 1. OneCycleLRのmax_lr調整
現在の実装では、`onecycle_max_lr`が`learning_rate * 2.0`になっているが、ファインチューニングではより高い値が必要。

### 2. Patienceの調整
ファインチューニングでは初期性能が高いため、Patienceを増やす必要がある。

### 3. 学習率スケジューラーの選択
OneCycleLRの代わりにCosineAnnealingLRやReduceLROnPlateauを試す。

## 📈 期待される改善

- **目標**: Macro F1 = 0.730 → **0.735以上**
- **テストセット**: Macro F1 = 0.6578 → **0.670以上**

## 🚀 次のステップ

1. 戦略2（バックボーン凍結）を実行
2. 結果を確認
3. 必要に応じて戦略1または戦略4を試す
4. 最良の結果を選択
